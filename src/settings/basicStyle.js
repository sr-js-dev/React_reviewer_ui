const rowStyle = {
  width: '100%',
  display: 'flex',
  flexFlow: 'row wrap',
};
const colStyle = {
  marginBottom: '16px',
};
const gutter = 16;
const gridStyle = {
  width: '25%',
  textAlign: 'center',
};
const basicStyle = {
  rowStyle,
  colStyle,
  gutter,
  gridStyle,
};

export default basicStyle;
